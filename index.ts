const isOpen: boolean = false;
const age: number = 155;
const myName: string = 'Alex';
const list: number[] = [1, 2, 3, 4];
const me: [string, number, boolean] = ['Alex', 20000, true];

enum Job {
    webDev,
    Front,
    Back
}

const job: Job = Job.webDev;


const phone: any = "iPhone";

const never